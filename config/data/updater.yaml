client: vietstock
symbols: ${data.discovery.symbols.vn30}
resolutions:
  - "${data.discovery.resolutions.vietstock.daily}"
  - "${data.discovery.resolutions.vietstock.weekly}"
  - "${data.discovery.resolutions.vietstock.monthly}"
