defaults:
    - _self_
    - app_config
    # discovery — các hằng số cho client discovery
    # symbols được load tự động bởi discover_symbols() trong config_utils.py
    - data/discovery/resolutions@data.discovery.resolutions.vietstock: vietstock
    # Data module
    - data@data.updater: updater
